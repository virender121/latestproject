import React from 'react'
import Counter from 'remote/Counter'

export const Compute = () => {
    return (
        <div>Compute
            <Counter />
        </div>

    )
}
export default Compute;
